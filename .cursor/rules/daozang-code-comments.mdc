---
description: 中文注释规范
globs: **/*.ts,**/*.tsx
alwaysApply: false
---

# 注释规范

- 新增或修改的代码必须包含清晰的中文注释。
- 注释解释「为什么」而不是「做什么」：说明组件为何这样拆分、设计为何这样取舍、约束来自哪里。禁止「定义函数」「返回结果」这类逐行翻译式注释。
- 每个新组件/模块文件顶部用块注释说明其职责边界与拆分理由（参考 `components/reader/BlockRenderer.tsx`、`lib/text-parser.ts` 的写法）。
- 历史遗留的英文注释不要求批量重写，改到哪个文件就顺手更新哪个文件。
